<launch>
  
  <!-- EXAMPLE: python script node -->
  <!--node name="arb_node_name" pkg="pkg_name" type="main_code_file" output="screen"     -->

  <!-- EXAMPLE: include launch file --> 
  <!-- include file="launch/file/location/file.launch"   ->
  
  <!-- Start the VRPN Client -->
  <include file="$(vrpn_client_ros)/launch/ohrad.launch"/>

  <!-- Start coms to PixHawk via Mavros -->
  <include file="$(mavros)/launch/px4.launch"/>

  <!-- Start the camera node -->
  <node name="usb_cam" pkg="usb_cam" type="usb_cam_node" output="screen" >
    <param name="video_device" value="/dev/video0" />
    <param name="image_width" value="320" />
    <param name="image_height" value="240" />
    <param name="pixel_format" value="yuyv" />
    <param name="camera_frame_id" value="usb_cam" />
    <param name="io_method" value="mmap"/>
    <param name="framerate" value="60"/>
  </node>

  <node name="image_view" pkg="image_view" type="image_view" respawn="false" output="screen">
    <remap from="image" to="/usb_cam/image_raw"/>
    <param name="autosize" value="true" />
  </node>

  
  <!-- Start the optic flow node -->
  <!-- Input = raw camera image, Output = optic flow ring --
.


  <!-- Start the oflow process node-->
  <!-- Input = optic flow ring, Output = desired yaw rate -->


  <!-- Control commands processing -->
  <!-- Input = desired yaw rate and altitude, Output = attitude commands -->

  <!-- Start the control command publish node-->
</launch>
